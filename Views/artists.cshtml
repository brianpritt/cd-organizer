<h1>Artists</h1>
@if (Model.Count == 0)
{
  <h2>You have no artists listed!</h2>
}

@foreach (var artist in Model)
{
  <p><a href="artists/@artist.GetId()">@artist.GetName()</a></p>
}
<br>
<h3><a href='/artists/new'>Add a new Artist</a></h3>
